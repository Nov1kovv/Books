package com.example.books

import org.junit.Test

import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}

// TODO: Покрыть тестаим все экраны на авторизацию с помощью Mockk (это такая библиотека)

/**
 * Суть в том что когда ты вызываешь какой то метод вью модели у тебя дергается определнный метод репозитория
 * Не приоритетная задача делай в самом конце я подскажу видосы и примеры
 * */


